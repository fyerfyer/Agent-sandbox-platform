# Test dependencies (install on top of requirements.txt)
pytest>=8.0
pytest-asyncio>=0.23
sseclient-py>=1.8   # for SSE streaming in E2E tests
requests>=2.31      # for HTTP-based platform E2E tests
python-dotenv>=1.0  # for loading .env in real LLM tests
httpx>=0.27         # for async HTTP calls (platform_tool + E2E tests)
